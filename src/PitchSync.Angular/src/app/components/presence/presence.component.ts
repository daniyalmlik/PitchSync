import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-presence',
  standalone: true,
  imports: [CommonModule],
  template: `<p>Presence — coming in Phase 7</p>`
})
export class PresenceComponent {}
